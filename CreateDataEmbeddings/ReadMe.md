# CreateDataEmbeddings

Creates the embeddings for the description and writes the embeddings vector back into the .xlsx. 

The .xlsx must be of shape:

Point(not used);Name;Link;Description;Embeddings
